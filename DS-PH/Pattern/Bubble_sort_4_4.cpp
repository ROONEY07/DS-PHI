#include<bits/stdc++.h>
using namespace std;
int main()
{
    /*
        Here Data is an array with n elements. This algorithom Sorts the 
        array Data

        1. Repeat 2 and 3 for k=1 to n-1
        2. set ptr = 1
        3.  Repeat while (ptr<n-k)
        4.         if(Data[ptr] > Data[ptr+1])
        5.                interchange Data[ptr], Data[ptr+1]
        6.   ptr++
             out of inner loop
        out of outer loop
        7. exit
    */
   int n;
   cout<<"Enter the size of array: ";
   cin>>n;

   // Declare an array
   int Data[n];
   cout<<"Enter the array: ";
   for (int i = 1; i <=n; i++)
   {
    /* code */
   }
   
}